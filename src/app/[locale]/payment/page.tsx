import { TossPayment } from '@/components/spaceDetail/Tosspayment';

export default function Payment() {
  return (
    <section className="flex h-screen w-full items-center justify-center">
      <TossPayment />
    </section>
  );
}
